[build]
  command = ""              # Blank = no build needed for your static site
  publish = "/"             # Or "." if root has your index.html

[functions]
  directory = "netlify/functions"  # Points exactly to your folder
